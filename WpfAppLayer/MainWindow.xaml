<Window x:Class="WpfAppLayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppLayer"
        mc:Ignorable="d"
        Title="Auto Login" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Chọn tài khoản: " Grid.Row="0"/>
        <ListBox x:Name="AccountListBox" Grid.Row="1" Margin="0,5,0,10"
                 SelectionChanged="AccountListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding DisplayName}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="LoginButton" Content="Đăng nhập" 
                    Click="LoginButton_Click" Width="100" Height="30" Margin="0,0,10,0"/>
            <Button x:Name="ManageAccountsButton" Content="Quản lý tài khoản" 
                    Click="ManageAccountsButton_Click" Width="120" Height="30"/>
        </StackPanel>

        <Button x:Name="SetPositionButton" Content="Đặt vị trí Username" Grid.Row="3"
                Click="SetPositionButton_Click" Width="150" Height="30" IsEnabled="False"/>
    </Grid>
</Window>
